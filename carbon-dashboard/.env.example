# URL of your FastAPI backend (the one in /python-backend/main.py).
# Leave empty to use same-origin requests (e.g. when both are reverse-proxied
# under one domain). Do NOT include a trailing slash.
VITE_API_BASE_URL=http://localhost:8000
